- name: Install System Packages 1 (build-essential, cmake, pkg-config, pthread)
  become_user: root
  apt:
    name: ["build-essential", "cmake", "pkg-config", "libpthread-stubs0-dev", "pipx"]
  tags:
    - install
    - neovim
    - core
- name: Install System Packages 2 (lua, unzip, libtool, libtool-bin, gettext)
  become_user: root
  apt:
    name: [ "lua5.1", "unzip", "libtool", "libtool-bin", "gettext", "direnv", "python3", "python"]
  tags:
    - install
    - neovim
    - core
- name: Install System Packages 3 (the rest)
  become_user: root
  apt:
    name: ["curl", "htop", "i3", "python3-pip", "dconf-editor", "nvidia-settings", "pavucontrol"]
  tags:
    - install
    - core
